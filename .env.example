# 環境變數範例檔案
# 複製此檔案為 .env 並根據你的環境修改配置

# Flask 應用配置
FLASK_APP=app.py
FLASK_ENV=development
FLASK_DEBUG=False
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# AI 後端服務配置
# 注意：如果使用 Docker，請使用容器名稱或 Docker 內網 IP
# 範例: http://ai-service:5000/api/remove_bg 或 http://172.17.0.3:5000/api/remove_bg
AI_BACKEND_URL=http://172.17.0.3:5000/api/remove_bg
AI_REQUEST_TIMEOUT=30

# CORS 配置
# 生產環境建議設定具體的前端網址，例如: http://localhost:3000
CORS_ORIGINS=*

# 檔案處理配置
# MAX_CONTENT_LENGTH 單位為 bytes (預設 16MB)
MAX_CONTENT_LENGTH=16777216
ALLOWED_EXTENSIONS=png,jpg,jpeg,gif

# 輸出檔案路徑
OUTPUT_DIR=./output
